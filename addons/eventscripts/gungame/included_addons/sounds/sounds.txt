import es
import string
import os

def GetSoundPack():
     if os.path.exists(os.getcwd() + "/cstrike/cfg/gungame/sound_packs/" + gg_sound_pack + ".txt")
         SoundPack = open(os.getcwd() + "/cstrike/cfg/gungame/sound_packs/" + gg_sound_pack + ".txt","r")
     else:
         if os.path.exists(os.getcwd() + "/cstrike/cfg/gungame/sound_packs/default.txt")
             SoundPack = open(os.getcwd() + "/cstrike/cfg/gungame/sound_packs/default.txt","r")
         else:
             # PLACE BACKUP CODE HERE
             SoundPack = open(os.getcwd() + "/cstrike/cfg/gungame/sound_packs/default.txt","r")

     for SoundPackLine in SoundPack.readlines():
          if not "//" in SoundPackLine:
          SoundPackLine=string.rstrip(SoundPackLine)
          SoundPackLine=string.split(SoundPackLine)
          if SoundPackLine != []:
               if SoundPackLine[0] != "gg_songlist":
                   if os.path.exists(os.getcwd() + "/cstrike/sound/gungame/" + gg_sound_pack + "/" + SoundPackLine[1])
                       es.set(SoundPackLine[0], SoundPackLine[1])
               else:
                   SoundPackSongList=string.split(SoundPackLine[1], ",")
                   for SoundPackSong in SoundPackSongList:
                       if not os.path.exists(os.getcwd() + "/cstrike/sound/gungame/" + gg_sound_pack + "/" + SoundPackSong)
                           SoundPackLine[1].replace(SoundPackSong + ",", "", 1)
                   if SoundPackLine[1] != "":
                       es.set(SoundPackLine[0], SoundPackLine[1])
                   else:
                       es.set("gg_rand_song", 0)